export type BackgroundColorProps = {
  bg?: string;
  bgImg?: {
    url: string;
    attachment?: string;
    position?: string;
    repeat?: string;
    size?: string;
  };
};

export type ColorProps = {
  fg?: string;
};
